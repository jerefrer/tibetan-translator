<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme (dark mode). -->
    <style name="Theme.tibetan_translator" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Status bar color matches the app navbar in dark mode -->
        <item name="android:statusBarColor">#000000</item>
        <item name="android:navigationBarColor">#000000</item>
        <!-- Make status bar icons light (for dark status bar background) -->
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
